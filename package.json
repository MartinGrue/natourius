{
  "name": "staticsites",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run-all": "npm-run-all --parallel compile:sass live-server",
    "compile:sass": " node-sass sass/main.scss css/style.css -w",
    "live-server": "npx live-server --port=8004",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "1.1.0",
    "node-sass": "^6.0.0"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
